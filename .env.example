# Supabase project that stores the editable content.
# Both values are safe to expose in the browser: the anon key only grants what
# the row-level security policies allow (read published rows; write nothing).
VITE_SUPABASE_URL=https://haxligazhfpusdzrpxrp.supabase.co
VITE_SUPABASE_ANON_KEY=
